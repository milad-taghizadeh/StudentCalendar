// show calender for datepicking and add event
jalaliDatepicker.startWatch();
const addEventDate = document.getElementById('evdate');
addEventDate.addEventListener("click" , function(e) {
    jalaliDatepicker.show(addEventDate);
})